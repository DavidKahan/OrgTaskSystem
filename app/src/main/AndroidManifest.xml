<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shenkar.orgtasksystem">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".AnalyticsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <meta-data -->
        <!-- android:name="com.parse.APPLICATION_ID" -->
        <!-- android:value="irUzywS69MpTb5AXjCsamv3vUr7Oh39xzkZJzkto" /> -->
        <!-- <meta-data -->
        <!-- android:name="com.parse.CLIENT_KEY" -->
        <!-- android:value="rTbKss08HgSA1zrhmSCVPPsn989IlaJsmK1OWRNY" /> -->
        <activity
            android:name=".views.SplashActivity"
            android:label="@string/ots">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".views.LoginActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".views.AddMembersActivity"
            android:label="@string/title_activity_create_team"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.CreateEditTaskActivity"
            android:label="@string/title_activity_create_edit_task"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.ReportTaskStatusActivity"
            android:label="@string/title_activity_report_task_status"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.TeamNameActivity" />
        <activity
            android:name=".views.NotificationActivity"
            android:excludeFromRecents="true"
            android:taskAffinity=""></activity>
        <activity android:name=".AnalyticsApplication"></activity>
    </application>

</manifest>
